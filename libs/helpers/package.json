{
  "private": true,
  "version": "1.0.0",
  "name": "@feature-toggles/helpers",
  "author": "Nicola Bello <nicola.bello.3@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/nicolabello/feature-toggles",
    "directory": "libs/helpers"
  },
  "license": "GPL-2.0-or-later",
  "files": [
    "build/**"
  ],
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "rimraf ./build/**.* && cp .eslintrc.js build/ && tsc",
    "build:watch": "rimraf ./build/**.* && cp .eslintrc.js build/ && tsc --watch"
  },
  "dependencies": {
    "axios": "^0.19.2"
  },
  "devDependencies": {
    "@nicolabello/material-components-web": "^8.0.11",
    "rimraf": "^3.0.0",
    "typescript": "^3.8.2"
  }
}
